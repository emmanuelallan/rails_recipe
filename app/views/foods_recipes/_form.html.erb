<%= form_with(model: foods_recipe) do |form| %>
  <% if foods_recipe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(foods_recipe.errors.count, "error") %> prohibited this foods_recipe from being saved:</h2>

      <ul>
        <% foods_recipe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :food_id, style: "display: block" %>
    <%= form.select :food_id, @foods.map { |t| [t.name, t.id]}, {}, class: "form-control" %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.text_field :quantity %>
  </div>

  <div>
    <%= form.text_field :recipe_id, value: recipe_id, style: "display: none" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
